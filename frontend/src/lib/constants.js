/**
 * Application-wide constants.
 * Single source of truth for values shared across components.
 */

// No default contract — all operations require an explicit active contract.
// null means "no contract selected".
export const DEFAULT_CONTRACT = null;
